# The '-' means exclude but it's actually optional since this file
# is specified with --exclude-from
# We always specify --one-file-system as well.
- /lost+found
- /var/log
- /var/tmp
- /var/lib
- /swapfile
- /tmp
- /minecraft/*/backups
- /minecraft/*/logs
- /home/freethenation
- /home/richard
# Just to make sure this list and the backup programs are included no matter how badly we screw up
+ /var/local/*
